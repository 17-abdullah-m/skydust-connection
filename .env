# DATABASE_URL is not set here on purpose.
# Vercel Preview + Production already have DATABASE_URL — adding it again
# from this file causes: "already exists for the targets preview and production".
# Local Next.js uses .env.local. Local Prisma CLI uses prisma/.env.
